% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opus_decode.R
\name{opus_decode}
\alias{opus_decode}
\title{Decode an Opus audio file to WAV}
\usage{
opus_decode(
  input,
  output,
  rate = 0L,
  stereo = FALSE,
  gain_db = 0,
  float = FALSE
)
}
\arguments{
\item{input}{Path to the input \code{.opus} file, or \code{"-"} for stdin.}

\item{output}{Path to the output \code{.wav} file.}

\item{rate}{Integer. Target sample rate in Hz. \code{0} (default) keeps
the stream's original rate (falling back to 48000 if unknown).}

\item{stereo}{Logical. Force stereo output. Default \code{FALSE}.}

\item{gain_db}{Numeric. Manual gain adjustment in dB. Default \code{0}.}

\item{float}{Logical. Write 32-bit IEEE float WAV instead of 16-bit PCM.
Default \code{FALSE}.}
}
\value{
Invisibly returns \code{0} on success, \code{1} on failure.
}
\description{
Decode an Opus audio file to WAV
}
\examples{
library(audio.opus)
file <- system.file(package = "audio.opus", "extdata", "test_opus.opus")
opus_decode(input = file, "test_opus.wav")
\dontrun{
  opus_decode("speech.opus", "speech.wav")
  opus_decode("music.opus",  "music.wav", rate = 44100, stereo = TRUE)
  opus_decode("audio.opus",  "audio.wav", gain_db = -3, float = TRUE)
}
}
